# Demo Scene (coming soon)

This folder will hold the FishNet Network Tools demo: a small host-mode scene whose
NetworkObjects carry SyncTypes that change over time, so you can watch the inspector
update live.

It ships as a Unity Package Manager sample. Once available, import it from
**Package Manager > FishNet Network Tools > Samples**.

_Placeholder so the folder is tracked. The scene and its demo behaviour land in a later build step._
